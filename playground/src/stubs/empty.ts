// Stub for Node.js built-in modules that are not needed at runtime in the browser.
// ESLint's Linter class does not use these — only the full ESLint class (file system access) does.
const empty = {};
export default empty;
export const promises = {};
export const constants = {};
